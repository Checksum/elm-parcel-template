{
    "name": "elm-netlify-template",
    "private": true,
    "license": "BSD-3-Clause",
    "description": "A GitHub template repository for Elm + ParcelJS apps",
    "author": "Srinath Sankar <srinath@iambot.net>",
    "scripts": {
        "prestart": "rm -rf dist",
        "start": "parcel src/index.html",
        "prebuild": "elm-test && rm -rf dist",
        "build": "parcel build src/index.html",
        "postbuild": "echo post build steps here like copying assets"
    },
    "devDependencies": {
        "elm": "^0.19.1-3",
        "elm-hot": "^1.1.4",
        "elm-test": "^0.19.1",
        "parcel": "^1.12.4",
        "elm-format": "^0.8.3"
    }
}
